name = "contact"
main = "src/index.ts"
compatibility_date = "2026-08-01"

[vars]
CONTACT_FROM = "contact@jeromefaria.com"
CONTACT_TO = "jerome.faria@gmail.com"
NEWSLETTER_FROM = "Jerome Faria <newsletter@jeromefaria.com>"
SITE_URL = "https://jeromefaria.com"
# Production origins only. Local dev adds http://localhost:5173 via .dev.vars
# (gitignored) so a Turnstile token minted on localhost can't pass the prod hostname pin.
ALLOWED_ORIGINS = "https://jeromefaria.com"

# Secrets (set with `wrangler secret put`, never committed):
#   TURNSTILE_SECRET   — Cloudflare Turnstile secret key
#   RESEND_API_KEY     — Resend API key

# Newsletter subscriber store. Create once with `wrangler d1 create newsletter`,
# then paste the printed database_id below and apply schema.sql (see README).
[[d1_databases]]
binding = "DB"
database_name = "newsletter"
database_id = "81db08d3-ebf6-4eaf-908b-ec6de0d6e82f"

# Rate limiting — native binding, no external resource to provision.
# 5 requests per 60s per client IP; the Worker fails open if this is absent.
[[unsafe.bindings]]
name = "RATE_LIMITER"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 5, period = 60 }
